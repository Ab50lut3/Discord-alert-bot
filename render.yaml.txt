services:
  - type: worker
    name: join-bot1
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python join_bot1.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: YOUR_DISCORD_USER_ID
        sync: false